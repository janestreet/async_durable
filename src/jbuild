(library
 ((name async_durable)
  (public_name async_durable)
  (preprocess (pps (ppx_jane -check-doc-comments ppx_driver.runner)))
  (libraries
   (async_kernel
    async_rpc_kernel
    async_unix
    core))))


(jbuild_version 1)
